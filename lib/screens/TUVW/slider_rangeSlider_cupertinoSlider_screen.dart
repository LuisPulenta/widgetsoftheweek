import 'package:flutter/material.dart';

class SliderRangeSliderCupertinoSliderScreen extends StatelessWidget {
  const SliderRangeSliderCupertinoSliderScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Slider RangeSlider CupertinoSlider'),
        centerTitle: true,
      ),
      body: const Center(
        child: Text('SliderRangeSliderCupertinoSliderScreen'),
      ),
    );
  }
}
